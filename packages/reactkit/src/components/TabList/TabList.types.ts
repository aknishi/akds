export interface TabListProps extends React.HTMLAttributes<HTMLDivElement> {
  /** Content rendered inside the tab list — typically Tab components. Required — overrides the optional inherited type. */
  children: React.ReactNode;
}
